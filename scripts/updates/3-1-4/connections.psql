ALTER TABLE connections ADD COLUMN created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP;
UPDATE connections SET created_at = last_found;
COMMENT ON COLUMN connections.created_at IS 'Record creation time';
